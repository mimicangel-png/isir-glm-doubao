# 2026-08-18 工作日志

## A股评分日报（10:14 触发）
- 引擎 v3.3 一次跑通（449/450 标的、共识 4 只、VCP 突破 19、超跌反弹 50）
- 邮件发送失败：`.env` 的 `SMTP_PASS=请填写QQ邮箱SMTP授权码` 是中文占位符，smtplib.login 在 SACRF2 编码阶段炸 ascii codec（位置 18-20 = `请填` 中文字节）
- 修 `send_report.py` 加 ASCII 校验 + Header/formataddr，错误信息从"ascii codec can't encode"模糊报错 → 直接定位"SMTP_PASS 含非 ASCII 字符，请到 QQ 邮箱→设置→账户→SMTP 服务生成 16 位授权码"
- **待左戈手动填授权码**（私密凭据不能凭空写）

## 文件改动
- `send_report.py` — 加 Header/formataddr + ASCII 校验
- `output/unified_2026-08-18.html` (6.6MB)
- `output/unified_2026-08-18.json` (1.0MB)
