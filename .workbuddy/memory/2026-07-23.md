# 2026-07-23

## 深度审查 + 6个严重bug修复
- **max_dd_20d方向反转** (CRITICAL): higher_better=False导致大回撤股票得高分 → 改为True
- **pe/pb_percentile假分位** (CRITICAL): 都是100-pct_52w造成动量三重计数(权重0.247) → 改用真实PE/PB
- **sector_rsi不是板块RSI** (CRITICAL): 只是rsi_val-50与rsi_signal冗余 → 改为计算同板块股票平均RSI
- **新高新低永远为0** (CRITICAL): k[-5:]包含今日 → 改为k[-5:-1]排除今日
- **calc_macd 71倍性能浪费** (CRITICAL): 每次从头算EMA → 改为增量EMA, 运行时间32s→14s
- **回测45.7%权重丢失**: turnover_z(0.451)等7个因子因std=0被跳过 → 需在回测中补充历史数据

## 修复后回测结果变化
- ISIR: 57.8%→56.4% (略降, 仍最优 👑)
- GLM: 56.0%→44.0% (大幅下降! 之前受益于动量三重计数bug)
- 豆包: 52.5%→52.2% (基本不变)
- 结论: GLM之前的高分部分来自bug, ISIR的选股能力更真实

## 其他已知问题(待修复)
- 回测中fund_flows/events为空, 7个因子(权重0.678)被丢弃
- 实盘events永远为空(传{})
- vol_price回测/实盘定义不一致
- 基本面因子(roe/gross_margin/ocf)硬编码50
- 回测K线过滤O(n*d*k)可优化

## 二轮深度审查 + 8个修复
- **PE/PB缺失值** (严重): pe=0时pe_percentile=100(最差) → 改为50(中性)
- **NaN传播** (严重): 标准化时NaN导致排名崩溃 → 加nan_to_num + isNan检查
- **除零保护** (严重): ret_5d/10d/20d和gap_open可能除零 → 加closes[x]>0守卫
- **北交所前缀** (严重): 832571/920808用了sz/sh前缀 → 添加bj前缀
- **K线解析崩溃** (严重): 单条坏数据崩溃整批 → 每条K线单独try/except
- **JSON容错** (中等): 历史文件损坏直接崩溃 → 加try/except重置
- **streak无上限** (中等): 连涨20天拉偏z-score → cap到10
- **胜率显示错误** (严重): win_count只统计最近50笔但total_count是全部 → 改为统计全部交易

## 仍存在的问题(后续迭代)
- 回测中56.2%权重因子被零化(turnover_z/mcap/pe/pb/fund_flows/events)
  → 需用today_mcap/today_close反推shares_outstanding计算历史值
- 实盘events永远为空
- fund_flows在GitHub Actions不可用(Node.js脚本路径硬编码)
- 卖出价不一致(实盘用收盘价, 回测用T+1开盘价)
- sector_rsi/momentum O(K^2)可预计算优化

## 三轮修复（算法+缓存+稳定性）
- **同日卖出再买入漏洞** (致命): 止损后立即重新买入导致止损失效 → 添加sold_today排除
- **RSI阈值不适用A股** (严重): RSI>80奖励+12在A股等于追高 → 改为>85扣分(超买风险), <30加分(超卖反弹)
- **CMF评分不对称** (中等): 微流出不扣分但微流入加分 → 添加elif cmf<0: -3
- **composite公式零化负收益** (严重): max(0,avg20)使亏损策略与打平策略同分 → 改为avg20*3不截断
- **inflow_rate恒为0** (严重): 从腾讯行情API获取流通市值计算
- **死代码清理**: 删除macOS硬编码Node.js路径、重复CSS、用_rsi替代rsi_signal反推
- **回测反推股本**: 用today_mcap/today_close计算shares → turnover_z和log_mcap终于参与回测

## 修复后回测结果
- ISIR: 61.9%胜率 +13.3%均收益
- GLM 👑: 61.4%胜率 +13.5%均收益 (composite更高因为均收益权重增大)
- 豆包: 49.6%胜率 +5.1%均收益 (RSI阈值调整后下降)
- 共识标的: 002396 星网锐捷 (三个体系都排名前5)
- 回测K线过滤可用bisect二分查找优化
